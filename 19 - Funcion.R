
# Crear una funcion

calculo_area <- function(width, height) {
  return(width * height)
}

resultado <- calculo_area(100, 50)  # se usa la fucion y se especifican los 
                                    # argumentos de entradas
resultado